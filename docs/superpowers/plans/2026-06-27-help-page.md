# 帮助页内嵌使用说明 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 WebhookSender App 底部导航新增“帮助”页，将钉钉 AI 表格 Webhook 配置教程嵌入界面，并升级版本号到 1.0.4。

**Architecture:** 在现有 `MainScreen.kt` 的 `Screen` 枚举中新增 `Help`，底部 `NavigationBar` 增加第四项。新增 `HelpScreen()` Composable 展示静态教程内容。文案集中放入 `res/values/strings.xml`。版本号在 `build.gradle.kts`、`SettingsDialog`、`docs/使用说明书.md` 三处同步更新。

**Tech Stack:** Kotlin, Jetpack Compose, Material3, Android resources.

---

## File Structure

| File | Responsibility |
|---|---|
| `app/src/main/java/com/example/webhooksender/ui/screen/MainScreen.kt` | 修改 Screen 枚举、导航项映射、新增 HelpScreen Composable |
| `app/src/main/res/values/strings.xml` | 新增帮助页所有文本资源 |
| `app/build.gradle.kts` | 升级 `versionCode` 和 `versionName` |
| `docs/使用说明书.md` | 同步更新“当前版本”文本 |

---

### Task 1: 更新版本号

**Files:**
- Modify: `app/build.gradle.kts`
- Modify: `docs/使用说明书.md`

- [ ] **Step 1: 修改 `app/build.gradle.kts`**

```kotlin
defaultConfig {
    applicationId = "com.example.webhooksender"
    minSdk = 26
    targetSdk = 35
    versionCode = 5
    versionName = "1.0.4"

    testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    vectorDrawables {
        useSupportLibrary = true
    }
}
```

- [ ] **Step 2: 修改 `docs/使用说明书.md` 第 205 行**

将：
```
- 当前版本: 1.0.3
```
改为：
```
- 当前版本: 1.0.4
```

- [ ] **Step 3: Commit**

```bash
git add app/build.gradle.kts docs/使用说明书.md
git commit -m "chore: 升级版本号到 1.0.4"
```

---

### Task 2: 添加帮助页文案到资源文件

**Files:**
- Modify: `app/src/main/res/values/strings.xml`

- [ ] **Step 1: 读取现有 `strings.xml` 确认根标签位置**

- [ ] **Step 2: 在 `</resources>` 前追加以下字符串资源**

```xml
    <!-- 帮助页 -->
    <string name="help_title">使用说明</string>
    <string name="help_app_desc_title">一、软件说明</string>
    <string name="help_app_desc">本软件可以将待办项直接发到钉钉的AI表格中，并进行记录。</string>
    <string name="help_steps_title">二、使用步骤</string>
    <string name="help_step_1">1、在钉钉的AI表格中新建“待办事项”文档；</string>
    <string name="help_step_2">2、在新建的文档中的自动化中新建工作流；</string>
    <string name="help_step_3_1">3.1 接收到数据时</string>
    <string name="help_step_3_1_1">3.1.1 把Webhook的地址记录下来待使用；</string>
    <string name="help_step_3_1_2">3.1.2 关键词1：填写“todos”；</string>
    <string name="help_step_3_1_3">3.1.3 参数格式：选择“Text”。</string>
    <string name="help_step_3_2">3.2 参数提取</string>
    <string name="help_step_3_2_1">3.2.1 用到的AI模型：随便选一个就可以；</string>
    <string name="help_step_3_2_2">3.2.2 待提取的文本：将“1.接收到数据时返回内容”的content写入txt中，将deadline写入time中，将priority写入pri中。注意：“1.接收到数据时返回内容”使用插入变量插入；</string>
    <string name="help_step_3_2_3">3.2.3 新建三个参数：txt是文本格式，time是日期时间格式，pri是文本格式。</string>
    <string name="help_step_3_3">3.3 新增记录</string>
    <string name="help_step_3_3_1">3.3.1 选择新建的待办事项数据表；</string>
    <string name="help_step_3_3_2">3.3.2 设置记录内容：待办事项选择“参数提取|txt”，截止时间选择“参数提取|time”，优先级选择“参数提取|pri”。</string>
    <string name="help_step_4">4、发布流程即可。</string>
    <string name="help_step_5">5、在本软件配置Webhook地址即可。</string>
```

- [ ] **Step 3: Commit**

```bash
git add app/src/main/res/values/strings.xml
git commit -m "feat: 添加帮助页字符串资源"
```

---

### Task 3: 新增 HelpScreen 并接入底部导航

**Files:**
- Modify: `app/src/main/java/com/example/webhooksender/ui/screen/MainScreen.kt`

- [ ] **Step 1: 导入 Help 图标**

在 `MainScreen.kt` 顶部 import 块中，确保已有：

```kotlin
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.*
```

该 import 已经存在，无需修改；Material3 的 `Icons.Filled.Help` 会通过星号 import 自动可用。

- [ ] **Step 2: 在 Screen 枚举中新增 Help**

```kotlin
enum class Screen { Send, History, Feedback, Help }
```

- [ ] **Step 3: 在底部 NavigationBar 中新增“帮助”项**

在现有第三个 `NavigationBarItem`（反馈）之后追加第四个：

```kotlin
NavigationBarItem(
    icon = { Icon(Icons.Default.Help, contentDescription = "帮助") },
    label = { Text("帮助") },
    selected = currentScreen == Screen.Help,
    onClick = { currentScreen = Screen.Help }
)
```

- [ ] **Step 4: 在屏幕切换映射中新增 Help 分支**

```kotlin
when (currentScreen) {
    Screen.Send -> SendScreen(sendViewModel, settingsViewModel) { showSettings = true }
    Screen.History -> HistoryScreen(sendViewModel)
    Screen.Feedback -> FeedbackScreen(sendViewModel)
    Screen.Help -> HelpScreen()
}
```

- [ ] **Step 5: 在文件末尾添加 HelpScreen 实现**

```kotlin
@Composable
private fun HelpScreen() {
    LazyColumn(
        modifier = Modifier
            .fillMaxSize()
            .padding(horizontal = 16.dp, vertical = 16.dp),
        verticalArrangement = Arrangement.spacedBy(14.dp)
    ) {
        item {
            Text(
                text = stringResource(id = R.string.help_title),
                style = MaterialTheme.typography.headlineSmall,
                fontWeight = FontWeight.Bold
            )
        }

        item { HelpCard(title = R.string.help_app_desc_title, body = R.string.help_app_desc) }

        item {
            Text(
                text = stringResource(id = R.string.help_steps_title),
                style = MaterialTheme.typography.titleMedium,
                fontWeight = FontWeight.Bold,
                modifier = Modifier.padding(top = 4.dp)
            )
        }

        item { HelpStepCard(stepTitle = R.string.help_step_1) }
        item { HelpStepCard(stepTitle = R.string.help_step_2) }
        item {
            HelpStepCard(
                stepTitle = R.string.help_step_3_1,
                subSteps = listOf(
                    R.string.help_step_3_1_1,
                    R.string.help_step_3_1_2,
                    R.string.help_step_3_1_3
                )
            )
        }
        item {
            HelpStepCard(
                stepTitle = R.string.help_step_3_2,
                subSteps = listOf(
                    R.string.help_step_3_2_1,
                    R.string.help_step_3_2_2,
                    R.string.help_step_3_2_3
                )
            )
        }
        item {
            HelpStepCard(
                stepTitle = R.string.help_step_3_3,
                subSteps = listOf(
                    R.string.help_step_3_3_1,
                    R.string.help_step_3_3_2
                )
            )
        }
        item { HelpStepCard(stepTitle = R.string.help_step_4) }
        item { HelpStepCard(stepTitle = R.string.help_step_5) }
    }
}

@Composable
private fun HelpCard(@StringRes title: Int, @StringRes body: Int) {
    Card(
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = Color(0xFFF5F5F5)),
        elevation = CardDefaults.cardElevation(defaultElevation = 1.dp)
    ) {
        Column(modifier = Modifier.padding(16.dp)) {
            Text(
                text = stringResource(id = title),
                style = MaterialTheme.typography.titleSmall,
                fontWeight = FontWeight.Bold,
                color = MaterialTheme.colorScheme.primary
            )
            Spacer(modifier = Modifier.height(6.dp))
            Text(
                text = stringResource(id = body),
                style = MaterialTheme.typography.bodyMedium
            )
        }
    }
}

@Composable
private fun HelpStepCard(
    @StringRes stepTitle: Int,
    subSteps: List<Int> = emptyList()
) {
    Card(
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = Color.White),
        elevation = CardDefaults.cardElevation(defaultElevation = 1.dp)
    ) {
        Column(modifier = Modifier.padding(16.dp)) {
            Text(
                text = stringResource(id = stepTitle),
                style = MaterialTheme.typography.titleSmall,
                fontWeight = FontWeight.Bold
            )
            if (subSteps.isNotEmpty()) {
                Spacer(modifier = Modifier.height(8.dp))
                subSteps.forEach { subStep ->
                    Text(
                        text = "• " + stringResource(id = subStep),
                        style = MaterialTheme.typography.bodyMedium,
                        modifier = Modifier.padding(bottom = 4.dp)
                    )
                }
            }
        }
    }
}
```

- [ ] **Step 6: 导入所需注解**

在 `MainScreen.kt` 顶部新增：

```kotlin
import androidx.annotation.StringRes
import com.example.webhooksender.R
```

- [ ] **Step 7: 修改 SettingsDialog 中的硬编码版本号为 1.0.4**

在 `SettingsDialog` 的 Column 内部，将：

```kotlin
Text(
    "版本：1.0.3",
    fontSize = MaterialTheme.typography.bodySmall.fontSize,
    color = Color.Gray
)
```

改为：

```kotlin
Text(
    "版本：1.0.4",
    fontSize = MaterialTheme.typography.bodySmall.fontSize,
    color = Color.Gray
)
```

- [ ] **Step 8: 构建验证**

```bash
./gradlew :app:assembleDebug
```

预期结果：BUILD SUCCESSFUL（至少没有编译错误；lint 警告不阻塞）。

- [ ] **Step 9: Commit**

```bash
git add app/src/main/java/com/example/webhooksender/ui/screen/MainScreen.kt
./gradlew :app:assembleDebug
git commit -m "feat: 新增帮助页并同步版本号显示"
```

---

### Task 4: 验证页面文案与版本号一致性

**Files:**
- Read: `app/src/main/res/values/strings.xml`
- Read: `app/build.gradle.kts`
- Read: `app/src/main/java/com/example/webhooksender/ui/screen/MainScreen.kt`

- [ ] **Step 1: 检查 strings.xml**

确认帮助页全部字符串已存在且和 `使用说明.txt` 内容一致：
- 软件说明
- 使用步骤 1~5
- 步骤 3.1 / 3.2 / 3.3 子项

- [ ] **Step 2: 检查 MainScreen.kt**

- `Screen` 枚举包含 `Help`
- `NavigationBar` 有四个 `NavigationBarItem`
- `when (currentScreen)` 包含 `Screen.Help -> HelpScreen()`
- `HelpScreen` 的实现调用了新加的字符串资源 ID

- [ ] **Step 3: 检查版本号一致性**

- `app/build.gradle.kts`: `versionCode = 5`, `versionName = "1.0.4"`
- `SettingsDialog`: 显示 `版本：1.0.4`
- `docs/使用说明书.md`: 当前版本为 `1.0.4`

- [ ] **Step 4: Commit 验证结果**

如果测试或手动阅读发现文案问题，修正后 commit；如果没有问题，不需要单独 commit。

---

## Spec Coverage Check

| 设计文档要求 | 实现任务 |
|---|---|
| 底部导航新增“帮助”页 | Task 3 Step 2-4 |
| 帮助页展示钉钉 AI 表格配置教程 | Task 2（文案） + Task 3 Step 5-6 |
| 不主动弹出首次引导 | 设计决策，实现中不做 |
| 纯 Compose 文本展示 | Task 3 Step 5 |
| 升级版本号到 1.0.4 | Task 1 |
| 版本号在多位置同步 | Task 1 + Task 3 Step 7 |

## Placeholder Scan

计划中无 TBD/TODO/fill in later 等占位符。每步均给出具体文件路径、代码片段和命令。

---

**Plan complete and saved to `docs/superpowers/plans/2026-06-27-help-page.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
